"""SteamView backend: media resolution for library entries.

Pure Python standard library. No third-party dependencies, by design --
this keeps the plugin ZIP buildable without Docker or the Decky CLI.

The public entry point is :class:`steamview.resolver.MediaResolver`.
"""

__version__ = "0.1.0"
